# Cross-document exposure is unsupported; the test waits for a remote toolchange event
# before opening a window.
[exposedTo-window-open.https.html]
  expected: TIMEOUT
  [Tools are scoped to a frame tree, and are not exposed across opener boundaries.]
    expected: TIMEOUT
