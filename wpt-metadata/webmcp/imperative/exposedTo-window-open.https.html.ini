# The new window does not inherit the polyfill; injection only reaches testharness pages.
[exposedTo-window-open.https.html]
  expected: TIMEOUT
  [Tools are scoped to a frame tree, and are not exposed across opener boundaries.]
    expected: TIMEOUT
