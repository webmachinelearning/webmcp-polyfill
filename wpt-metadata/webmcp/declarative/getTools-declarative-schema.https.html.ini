# Forms do not register tools, so the schema helper never finds a matching tool.
[getTools-declarative-schema.https.html]
  expected: TIMEOUT
  [WebMCP: getTools() retrieves declarative schema at registration time]
    expected: TIMEOUT
