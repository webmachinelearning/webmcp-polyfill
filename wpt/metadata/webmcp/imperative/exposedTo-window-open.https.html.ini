# WPT does not inject the polyfill into the window.open('about:blank') document, so its
# document.modelContext is undefined.
# Local tests cover opener boundaries with a participating popup.
[exposedTo-window-open.https.html]
  [Tools are scoped to a frame tree, and are not exposed across opener boundaries.]
    expected: FAIL
