# Declarative form registration is unsupported; waitForTool() in the first case never
# resolves, so the toolactivated case does not run.
[executeTool-pseudo-classes.https.html]
  expected: TIMEOUT
  [Pseudo-classes :tool-form-active and :tool-submit-active apply correctly during execution and are removed upon completion]
    expected: TIMEOUT
  [Form controls are populated with arguments when toolactivated event fires]
    expected: NOTRUN
