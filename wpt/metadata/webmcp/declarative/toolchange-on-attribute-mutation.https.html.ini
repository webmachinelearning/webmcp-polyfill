# Declarative form registration is unsupported; waitForTool() in the first case never
# resolves, so the later cases do not run.
[toolchange-on-attribute-mutation.https.html]
  expected: TIMEOUT
  [Test that mutating or removing form attributes (toolname, tooldescription, tooltitle, toolautosubmit) fires toolchange and updates getTools() correctly]
    expected: TIMEOUT
  [No toolchange event on unrelated attribute mutation]
    expected: NOTRUN
  [No toolchange event on redundant schema mutation]
    expected: NOTRUN
