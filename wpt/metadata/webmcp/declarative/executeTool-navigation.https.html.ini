# Declarative form registration is unsupported; waitForTool() never resolves.
[executeTool-navigation.https.html]
  expected: TIMEOUT
  [executeTool resolves with null and triggers standard form navigation]
    expected: TIMEOUT
