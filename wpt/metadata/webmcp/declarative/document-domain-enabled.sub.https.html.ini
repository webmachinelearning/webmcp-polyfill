# Safari 26.6.2 has no originAgentCluster; the later tools policy check rejects instead.
[document-domain-enabled.sub.https.html]
  [Declarative tool registration is blocked when document.domain is enabled]
    expected:
      if product == "safari": FAIL
