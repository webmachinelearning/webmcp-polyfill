# Declarative form registration is unsupported, and WPT does not inject the polyfill into
# script-created about:blank iframes, so the schema helper never resolves.
[sandboxed-iframe.https.html]
  expected: TIMEOUT
  [Declarative tool in sandboxed iframe without allow-scripts is not registered]
    expected: TIMEOUT
