# Declarative form registration is unsupported, and WPT does not inject the polyfill into
# script-created about:blank iframes, so waitForTool() never resolves. The three earlier
# cases pass only because no declarative tool is ever registered.
[no-frame-documents.https.html]
  expected: TIMEOUT
  [Declarative tool in iframe removed after registration does not crash on attribute modification]
    expected: TIMEOUT
