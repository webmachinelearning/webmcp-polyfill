# Declarative form registration is unsupported, so no mutation fires toolchange.
[toolchange-on-name-change.https.html]
  expected: TIMEOUT
  [Test that toolchange event fires on various mutations of form controls]
    expected: TIMEOUT
