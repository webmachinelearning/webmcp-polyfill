# Declarative form registration is unsupported; waitForTool() never resolves.
[form_removal_submit_crash.https.html]
  expected: TIMEOUT
  [Form removal on submit with respondWith does not crash the renderer]
    expected: TIMEOUT
