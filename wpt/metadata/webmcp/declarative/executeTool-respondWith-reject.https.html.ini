# Declarative form registration is unsupported; waitForTool() never resolves.
[executeTool-respondWith-reject.https.html]
  expected: TIMEOUT
  [executeTool rejects when respondWith() promise is rejected]
    expected: TIMEOUT
